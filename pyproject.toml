[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cigar-isci-reviewer"
version = "0.1.0"
description = "Reviewer reproduction code for the CIGAR iScience manuscript"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
